# NOTE: `name` is load-bearing beyond Dart. The msix builder uses it as the
# AppxManifest Application Id (with underscores stripped), and centroidx-manager
# hard-codes that id in the shell:AppsFolder URI it uses to relaunch the app
# after an update — see windowsAppID in
# tools/centroidx-manager/internal/platform/installer.go. Renaming this package
# without updating that constant makes an update appear to succeed while the
# HMI never comes back.
name: centroidx
description: "Centroid HMI system for your wildest dreams."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# Format: YYYY.MM.DD+1, +1 is the build number that day
version: 2026.8.23+1

environment:
  sdk: ^3.6.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.3
  open62541: ^1.5.7+3
  tfc:
    path: ../
  flutter_riverpod: ^2.6.1
  beamer: ^1.7.0
  shared_preferences: ^2.5.3
  json_annotation: ^4.9.0
  rxdart: ^0.28.0
  amplify_secure_storage_dart: ^0.5.6
  upgrader: ^11.5.1
  http: ^1.6.0
  path_provider: ^2.1.5
  centroidx_upgrader:
    path: ../packages/centroidx_upgrader
  # Video output for package:media_kit on the flutter-elinux stations, where
  # upstream's GTK plugin cannot be registered. No Dart API — depending on it
  # is what puts the native half into the eLinux build.
  media_kit_video_elinux:
    path: ../packages/media_kit_video_elinux
  marionette_flutter: ^0.4.0

dev_dependencies:
  flutter_native_splash: ^2.4.6
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  json_serializable: ^6.9.5
  msix: ^3.16.12


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # The theme's typeface (`kAppFontFamily` in lib/theme.dart), bundled so
  # every build draws it. DejaVu Sans is what stations have always drawn: it
  # is the only sans in their debian:trixie-slim container, which is where
  # fontconfig fell back to before any font was bundled. These are the files
  # fonts-dejavu-core 2.37-8 installs there, Regular and Bold only, as on a
  # station. The files live in the tfc package's lib/, which is why they are
  # addressed as packages/tfc/.
  fonts:
    - family: dejavu-sans
      fonts:
        - asset: packages/tfc/fonts/dejavu-sans/DejaVuSans.ttf
        - asset: packages/tfc/fonts/dejavu-sans/DejaVuSans-Bold.ttf
          weight: 700

assets:
  - assets/icon.png
  - assets/manager/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon_white_bg.png"
  windows:
    generate: true
    image_path: "assets/icon.png"
  macos:
    generate: true
    image_path: "assets/icon.png"
  linux:
    generate: true
    image_path: "assets/icon.png"

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/icon.png
  ios: true
  android: true

dependency_overrides:
  # The panel leaks 72-122 MiB/h and is OOM-killed on a station capped at
  # 1 GiB. Measured: a monitored item the server refuses -- which a key with a
  # renamed tag is, forever, on the retry ladder -- leaked its native callback
  # and the value map it captures on every rebuild, and `write()` closed its
  # callback only on the success path. Fixed in open62541_dart#119.
  #
  # **The big one is the serializer.** `OpcUaDynamicValueSerializer.deserialize`
  # calloc'd a 48-byte UA_ExtensionObject header for every struct-valued
  # variant and never freed it -- per struct notification, per read, and per
  # element of an array of structs. A station taking 380-393 notifications a
  # second leaks ~1.07M of those an hour, ~70 MB/h before glibc arena
  # fragmentation, which is the 97-110 MiB/h measured on a panel that is
  # OOM-killed every six to nine hours. `serialize` had the same pattern.
  # open62541_dart#121; found with a tcmalloc heap profile and confirmed
  # independently from the notification rate.
  #
  # Pinned by SHA rather than by branch, the way the other overrides in this
  # repository are: `pubspec.lock` is gitignored, so a branch ref lets a tagged
  # build pick up untested code. Head of `dev-leakfixes`, which is #119 and
  # #121 merged over main -- both fixes in one ref so a station takes one
  # rebuild rather than two.
  # This whole block goes when the fix is released to pub.dev and the
  # `open62541: ^1.5.7+3` constraint above can name that version instead.
  open62541:
    git:
      url: https://github.com/centroid-is/open62541_dart.git
      ref: 19b40be99f433bc9dca8bdcbed25999a6e39536a
  # tfc:
  #   git:
  #     url: https://github.com/centroid-is/tfc-hmi.git
  #     ref: main
  postgres:
    git:
      url: https://github.com/centroid-is/postgresql-dart.git
      # Head of `fix-orphaned-socket-on-failed-connect` as of 2026-08-23. This
      # branch moved on 2026-08-22, so it was the live one of the four.
      ref: 519312712bacca6c54f296ce190bdb85f37a2581
  open62541:
    git:
      url: https://github.com/centroid-is/open62541_dart.git
      # `monitor-quality-sourcetime`: 1.5.7+3 (main) plus DynamicValue
      # .statusCode / .sourceTimestamp and opt-in `deliverBadStatus`. Keep in
      # step with the root pubspec.yaml; every open62541 override in this
      # repo carries this same SHA.
      ref: 90281a6cf3ccca43ce79ac10a842cb680ea68f52
  # drift:
  #   path: ../../drift/drift
  # drift_postgres:
  #   path: ../../drift/extras/drift_postgres
  # drift_flutter:
  #   path: ../../drift/drift_flutter


msix_config:
  msix_version: 2026.8.23.1
  display_name: CentroidX
  description: 'Industrial HMI application, monitors and controls automated processing lines. Connects to OPC UA servers and PostgreSQL databases, gathers data and displays it in a user-friendly way.'
  # Must equal the signing certificate subject exactly, byte for byte: the
  # MSIX publisher IS the certificate subject. It is also what Windows reads
  # out on an approval prompt, which is why it is a name and no longer a GUID.
  # Changing it changes the package identity -- see scripts/generate-signing-cert.ps1.
  publisher: CN=Centroid, O=Centroid ehf., C=IS
  publisher_display_name: Centroid
  logo_path: assets/icon.png
  identity_name: Centroid.CentroidX
  architecture: x64
  capabilities: "internetClient,privateNetworkClientServer"
  store: false
  certificate_path: centroidx-sideload.pfx
  certificate_password: PLACEHOLDER_SET_IN_CI
